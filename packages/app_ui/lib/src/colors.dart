import 'package:flutter/widgets.dart';

/// Defines the color palette for the Vendease UI.
// ignore: prefer-match-file-name
abstract class VendeaseColors {
  /// Black
  static const Color black = Color(0xFF202124);

  /// White
  static const Color white = Color(0xFFFFFFFF);

  /// Primary
  static const Color primary = Color(0xFF135FDC);

  /// Secondary
  // ignore: use_full_hex_values_for_flutter_colors
  static const Color secondary = Color(0xff00000099);

  /// Charcoal
  static const Color charcoal = Color(0xBF202124);

  /// Unselected
  static const Color unselected = Color(0xFF8091B3);

  /// Divider
  static const Color divider = Color(0xFFF0F5FF);
}
